# LOCATION: shared/permissions/pyproject.toml

[project]
name = "insynchire-permissions"
version = "0.1.0"
description = "Canonical PERMISSION_MATRIX (FIX-M3) - single source of truth for role enforcement across all InSyncHire services"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["permissions*"]
